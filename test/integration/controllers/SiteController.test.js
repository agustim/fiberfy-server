'use strict'
/* global describe, it */

const assert = require('assert')

describe('SiteController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['SiteController'])
  })
})
